# Temporary Defers

## Anthropic adapter

Status: deferred intentionally for now.

Reason:
- current Slice 1 focus is to finish the scaffold around typed records, stage contracts, checkpointing, manifest/stage stats, and the OpenAI-compatible LLM client foundation
- OpenAI-compatible routing also leaves a practical near-term path for OpenRouter-based experimentation

When to revisit:
- after typed pipeline scaffolding is stable
- after run artifacts/report shape is closer to the approved spec
- before building features that explicitly need provider comparison or multi-provider judging
