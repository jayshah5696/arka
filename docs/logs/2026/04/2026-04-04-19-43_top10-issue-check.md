# Top 10 issue check

Date: 2026-04-04 19:43

Reviewed the requested issue list against current code.

## Summary

A few items are already fixed, but several are **still valid and should be acted on**.

## Item-by-item status

### 1. `complete_structured` is naive
**Status: partially fixed, not fully fixed**

Current state:
- no longer just raw `json.loads(text)`
- now extracts JSON from fences and embedded text via `_extract_json_text()`

But the core criticism still stands:
- it still calls `complete()` first
- it still depends on prompt discipline and regex extraction
- it is not yet using provider-native structured output / JSON mode

Verdict:
- **not fully fixed**
- should still be improved

### 2. Retry is fixed wait, not exponential backoff
**Status: still valid**

Current code uses:
- `wait_fixed(1)`

Spec expectation was exponential backoff.

Verdict:
- **still needs fixing**

### 3. No JSON format instruction in judge prompt
**Status: fixed enough for now**

Current prompt includes:
- `Return valid JSON only with this shape: ...`

That addresses the core issue.

Verdict:
- **fixed**

### 4. `StrictModel` defined multiple times
**Status: fixed**

Now centralized in:
- `src/arka/common/models.py`

Verdict:
- **fixed**

### 5. `openai_client.py` is dead code
**Status: still valid**

Current duplication remains:
- `src/arka/llm/openai_client.py`
- `_build_openai_client()` inside `src/arka/llm/client.py`

This is still a real cleanup target.

Verdict:
- **still needs fixing**

### 6. Anthropic adapter missing
**Status: still valid**

Current config still says:
- `provider: Literal["openai"]`

So despite the defer note, this is still a real spec gap.

Verdict:
- **still missing**

### 7. `Record.id = "seed-{row_index}"`
**Status: still valid**

Current `SeedSourceStage` still does:
- `id=f"seed-{row_index}"`

That is not content-stable.

Verdict:
- **still needs fixing**

### 8. `content_hash` truncated to 16 hex chars
**Status: still valid**

Current code still truncates to 16 chars in multiple places.

Verdict:
- **still needs fixing**

### 9. `complete_batch` worker count uncapped
**Status: mostly okay in usage, but concern is still directionally valid**

Current code:
- `worker_count = max_workers or len(batch) or 1`

If caller passes nothing and batch is large, thread count = batch size.
So yes, uncapped default still exists.

Verdict:
- **still needs fixing**

### 10. `PipelineRunner` has no per-stage failure handling
**Status: still valid**

Current runner:
- registers run as `running`
- runs stages without stage-level try/except
- updates to `completed` only at the end

If a stage throws, run status can indeed remain `running`.

Verdict:
- **still needs fixing**

---

## Final status table

| Issue | Status |
|---|---|
| 1. `complete_structured` naive | Partially fixed, still needs improvement |
| 2. retry not exponential | Still valid |
| 3. no JSON prompt instruction | Fixed |
| 4. duplicated `StrictModel` | Fixed |
| 5. dead `openai_client.py` | Still valid |
| 6. missing Anthropic adapter | Still valid |
| 7. unstable seed record id | Still valid |
| 8. short content hash | Still valid |
| 9. uncapped batch worker count | Still valid |
| 10. no stage failure handling | Still valid |

---

## Recommended immediate priority order

If fixing these now, I would go in this order:

1. **PipelineRunner failure handling**
2. **seed record id + full-length content hash**
3. **uncapped `complete_batch` worker count**
4. **deduplicate OpenAI client construction**
5. **retry backoff change to exponential/jitter**
6. **provider-native structured output path**
7. **Anthropic adapter**

## Why this order

- failure handling and identity correctness affect artifact trustworthiness immediately
- worker caps prevent resource blowups
- duplicate client construction is a clean consistency fix
- structured output and Anthropic are important, but can come just after the current runtime-safety issues
