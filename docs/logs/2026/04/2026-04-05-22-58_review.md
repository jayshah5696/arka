# Completion status

Short answer: mostly yes for the three requested gaps, with two caveats.

## Done
- Near-dedup stage added and wired into the pipeline
- Dataset report artifacts added:
  - `samples.jsonl`
  - `canaries.json`
  - `run_report.json` now includes samples/canaries/diversity fields
- `output.format` is now honored
  - `jsonl`
  - `chatml`
  - `alpaca`

## Caveats
1. Near-dedup currently implements the MinHash path only.
   - This matches the user’s explicit request (`Near-dedup (MinHash)`), but not the extra SimHash variant mentioned elsewhere in `SPEC.md`.

2. Diversity score is best-effort.
   - It uses real embeddings when available.
   - If the embedding call fails/unavailable, the field becomes `null` instead of failing the run.

## Validation
- Targeted tests pass
- Ruff check/format check pass

## Bottom line
- If the question is “did you implement the three requested gaps from the prompt?”: **yes, functionally yes**.
- If the question is “is every adjacent spec idea fully complete with no caveats?”: **not quite**.
