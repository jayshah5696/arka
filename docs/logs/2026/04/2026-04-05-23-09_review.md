# Commit summary

Confirmed: new tests were added/updated for the embeddings config change and related SFT completeness work.

## Test coverage added/updated
- `tests/unit/test_config_loader.py`
  - validates `embeddings.provider` and `embeddings.model`
  - validates OpenAI/OpenRouter-style embedding config parsing
- `tests/unit/test_reports_and_stage_events.py`
  - validates diversity-score path with HuggingFace/local embedding backend via monkeypatch
- also updated existing pipeline/config tests to include the new `embeddings` config shape where required

## Commit
- `095f93b` — `Add SFT report, formatter, and embedding config support`
