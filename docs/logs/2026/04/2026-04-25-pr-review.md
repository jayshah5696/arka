# PR Review — 2026-04-25

## Open PRs summary

| # | Title | Verdict | Action |
|---|-------|---------|--------|
| #43 | Bolt: fastembed embedding cache | ✅ Merge | Real feature, clean impl, tests updated |
| #42 | Sentinel: CRITICAL – API keys in config.resolved.yaml | ⚠️ Superseded by #40/#38 | Close (duplicate) |
| #40 | Sentinel: CRITICAL – API key leak via StageLLMOverride | ✅ Best of the security trio | Merge this one |
| #39 | Palette: Catch pipeline execution errors gracefully | ✅ Merge | Supersedes #37, broader catch |
| #38 | Sentinel: CRITICAL – Prevent API key leak in StageLLMOverride | ⚠️ Superseded by #40 | Close (duplicate, leaves junk test files) |
| #37 | Palette: Clean error for missing pipeline files | ⚠️ Superseded by #39 | Close (narrow FileNotFoundError only) |

## Detail notes

### #43 — fastembed embedding cache ✅ Merge
- Adds `embeddings_cache` SQLite table inside `CheckpointManager`
- `_embed_texts` now accepts optional `checkpoint_manager`, checks cache first
- Threaded through `_compute_diversity_score` and `SemanticSimilarityFilterStage.run`
- Tests updated with `checkpoint_manager=None` signature
- No junk files left behind (plan.md is benign)
- **One concern**: `import hashlib` is inside the method body — minor, but should be top-of-file. Not a blocker.

### #42 vs #40 vs #38 — Security: API key leak ⚠️
All three fix the same bug: `StageLLMOverride.api_key` missing `Field(exclude=True)`.

- **#42** — leaves 5 root-level test scripts (`test_exclude.py`, `test_json_enc.py`, etc.) committed to root. Not acceptable.
- **#38** — leaves `test_exclude_newer.py` at root. Not acceptable.
- **#40** ✅ — no junk files, broadest fix (also adds `Field(exclude=True)` to `EmbeddingsConfig`), proper sentinel.md, all 266 tests pass.
- **Merge #40, close #42 and #38.**

### #39 vs #37 — CLI error handling ⚠️
Both wrap `PipelineRunner.run()` in `cli.py` with an except block.

- **#37** — only catches `FileNotFoundError`. Too narrow; any other runtime error still bubbles as a raw traceback.
- **#39** ✅ — catches `Exception`, clean message to stderr, exits 1. Tests mock the runner directly. Supersedes #37.
- **Merge #39, close #37.**

## Recommended merge order

```
1. Merge #40  (security, no side effects)
2. Merge #39  (CLI DX, no side effects)
3. Merge #43  (feature, builds on clean base)
4. Close #42, #38, #37 as superseded
```
