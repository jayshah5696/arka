# Commit review fixes

Committed the review-fix pass for pipeline correctness and resume behavior.

## Included

- shared checkpoint-manager flow via `PipelineRunner` and `StageContext`
- resume gated by checkpoint status instead of parquet existence alone
- explicit seed source missing-path error
- labeling rubric missing-file error context
- language filter warning for unsupported non-English heuristic configs
- explicit rubric canary labels via `few_shot[].expected_verdict`
- generator checkpoint response count updates after each write
- diversity scoring decoupled from `ConversationRecord` internals
- source stage signatures widened to `list[Record]`
- tests and rubric fixture updates

## Validation

- `uv run pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
