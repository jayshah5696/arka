# Cleanup note

You asked why `.bak` files were there.

They were temporary safety copies I created while replacing the old root-level example config layout with the new `examples/` layout.

Current state:
- the `.bak` files have already been removed
- generated run artifacts were also removed from `examples/`
- downloaded sample PDF was removed from `examples/pdfs/`
- only source files that belong in the repo remain
