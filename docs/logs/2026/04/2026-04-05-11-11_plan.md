# Tier A slice execution: Evol-Instruct + PDF + IFD gate

Committed the earlier validation-matrix and plan-doc work, then executed the next plan slices in code.

## Commit created
- `f4ffe2b` — `Add validation matrix and Tier A slice plan`

## Implemented now

### Slice A1 — Evol-Instruct generator
- added `generator.type: evol_instruct`
- added config validation for:
  - `rounds >= 1`
  - `branching_factor >= 1`
  - non-empty supported operator set
- added supported operators:
  - `add_constraints`
  - `deepen`
  - `increase_reasoning_steps`
  - `breadth_mutation`
- added per-round `EvolInstructRoundStage`
- preserved frontier-only round behavior
- preserved accumulated outputs across rounds
- added lineage/source semantics for evolved outputs
- added drop reasons:
  - `evol_identical_to_parent`
  - `evol_refusal`
  - `evol_instruction_too_short`
  - `evol_edit_distance_too_small`
  - `evol_parse_failure`

### Slice A3 — PDF source stage
- added `data_source.type: pdf`
- added `GroundedChunkPayload` and `GroundedChunkRecord`
- added `PDFSourceStage` using `pypdf`
- implemented text-native extraction + fixed-size chunking + provenance fields
- made prompt-based generation accept both:
  - `ConversationRecord`
  - `GroundedChunkRecord`
- added example config: `config.examples.pdf-grounded.yaml`

### Slice A2 — IFD feasibility gate
- added `filters.ifd.enabled` config surface
- added `SequenceScore` boundary model
- added `LLMClient.score_response()` API boundary
- explicitly capability-gated IFD in StageBuilder
- current provider capability is marked unsupported for live execution, so enabling IFD fails fast with a clear error instead of using a fake approximation
- added `IFDFilterStage` and unit coverage for formula/stats behavior via injected fake scorer

## Docs/config updates
- added `config.examples.evol-instruct.yaml`
- updated `README.md`
- updated `docs/config-examples.md`

## Validation
Passed:
- `uv run pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`

Result:
- `162 passed`
- reported coverage: `92%`

## Important note
The IFD slice is intentionally stopped at the feasibility gate for real provider execution. The code now fails fast on unsupported response-scoring capability, which matches the plan and avoids pretending IFD is implemented end-to-end on the live backend.
