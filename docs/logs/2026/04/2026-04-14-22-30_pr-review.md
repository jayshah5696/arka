# Cleanest path recommendation

User asked for the cleanest path only.

## Cleanest path

1. **Merge/implement #20** for the NearDedup LSH performance fix.
2. **Merge/implement #17** for the real security hardening gap.
3. **Create one fresh consolidated DX PR** that combines:
   - readable config validation errors
   - `--dry-run`
   - run summary output
   - CLI help text

## Optional

4. **Only do #21 if you want `api_key` omitted entirely** from serialized config.
   Current `main` already masks `SecretStr` as `***`, so this is not urgent.

## Close everything else

Close the duplicate PRs after taking what you want from them.

## Why this is the cleanest path

- It reduces many overlapping PRs into **3 real workstreams**.
- It avoids merging duplicate agent-generated variants.
- It keeps security/perf improvements while minimizing review noise.
- It avoids merging feature-heavy or artifact-heavy PRs before product decisions.
