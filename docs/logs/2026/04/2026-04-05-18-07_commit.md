# Commit examples reorganization and humanizer workflow

Committed the examples reorganization and humanizer-focused SFT workflow changes.

## Included

- moved user-facing configs into `examples/`
- added `examples/README.md` and future placeholders
- added example seeds for CSV and humanizer rewriting tasks
- added example validation module and script
- updated tests for example validation and smoke fixture usage
- updated README, validation docs, and justfile
- retired old root-level example configs
- excluded generated runs, outputs, state db, and downloaded PDF from commit

## Validation

- `uv run pytest -q`
- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv run python scripts/validate_examples.py`
