# Plan Update: Strict TDD Refactoring

We have updated the implementation plan to incorporate strict test-driven development (TDD):

1. **Config Resolution**: Write a new test verifying `get_stage_config` on `Stage` (matching type, resolving filters list, and resolving legacy overrides) before moving stages to use it.
2. **Labeling Engine**: Add a test verifying that `label_batch` schedules the canary checks concurrently in the thread pool rather than sequentially.
3. **JSON Extraction**: Update the greedy regex test case to assert non-greedy extraction and add a test verifying trailing braces handling, then implement balanced-brace extraction.
4. **Legacy Config**: Deprecate legacy parsing at runtime and test the new offline CLI migration script.

The updated plan is saved at [implementation_plan.md](file:///Users/jshah/.gemini/antigravity/brain/3fd3c205-7737-4256-9419-44e413fecfe3/implementation_plan.md).
