# Completed Migration: argparse to click

We successfully completed the migration of all argparse CLI command wrappers and scripts to Click.

## Summary of Completed Tasks

1. **Click Dependency**: Added `click` dependency to `pyproject.toml` and synchronized the virtual environment using `rtk uv add click`.
2. **Core CLI (`src/arka/cli.py`)**: Refactored the command parsing and pipeline runner invocation to use Click. Kept a `main(argv)` wrapper to ensure backward compatibility and prevent exit code issues in pytest unit runs.
3. **Offline Config Migrator (`scripts/migrate_config.py`)**: Migrated to Click command structure with file path validation.
4. **Evaluation Tools (`tools/simula_eval/compare.py`, `tools/simula_eval/metrics.py`)**: Refactored both evaluation scripts to utilize Click CLI decorators.
5. **Linting and Formatting**: Fixed and formatted all codebase imports and styling using `rtk uv run ruff check --fix` and `rtk just format`.
6. **Testing and Verification**: All 331 tests passed successfully using `rtk just check`. Checked `--help` outputs and executed manual dry-run commands.
