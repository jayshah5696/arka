## 2023-10-26 - Added LSH to NearDedupStage / Learning: Pre-computing MinHash signatures along with LSH bucketing changes the bottleneck in deduplication from an O(N^2) comparisons stage to linear lookups, greatly optimizing performance for larger datasets. We can avoid unnecessary tokenization and signature generation. / Action: Remember to evaluate similar caching/bucketing opportunities in other similarity-search or clustering features.
