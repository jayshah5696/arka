## 2025-04-10 - [O(n^2) Bottleneck in NearDedupStage] / Learning: [Comparing every record against all cluster representatives in NearDedupStage is an O(n^2) operation, causing severe performance degradation at scale.] / Action: [Implement Locality-Sensitive Hashing (LSH) bucketing for MinHash signatures to group potential matches and reduce the number of Jaccard similarity comparisons to an average case of O(n).]
